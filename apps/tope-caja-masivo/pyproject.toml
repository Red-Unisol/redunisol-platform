[project]
name = "tope-caja-masivo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "pycryptodome",
    "openpyxl",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
